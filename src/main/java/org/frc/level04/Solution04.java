package org.frc.level04;

public class Solution04 {
    
    // YOU EDIT THIS FILE
    //
    // In this level, you will use a loop to simulate time passing.
    //
    // Big idea:
    //   - Each tick represents one unit of time
    //   - Each tick adds speed to total distance
    //
    // This is different from:
    //   distance = speed * ticks
    //
    // Here, we accumulate distance step-by-step.
    
    /**
     * Calculates total distance traveled by accumulating movement
     * over a number of ticks.
     *
     * For each tick:
     *   - distance increases by speed
     *
     * Example:
     *   speed = 3
     *   ticks = 5
     *   distance = 15
     *
     * This method should NOT print anything.
     *
     * @param speed units moved per tick
     * @param ticks number of time steps
     * @return total distance traveled
     */
    public static int calculateDistance(int speed, int ticks) {
        
        // STEP 1: create a variable to track total distance
        int distance = 0;
        
        // STEP 2: create a loop that runs once per tick
        // for (int i = 0; i < ticks; i++) {
        //
        //     STEP 3: add speed to distance each tick
        //     distance = ...
        //
        // }
        
        
        
        // STEP 4: return the total distance
        return distance;
    }
}
